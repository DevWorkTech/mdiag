# Подключать внутри http {}, например из /etc/nginx/conf.d/mdiag-log-format.conf.
# $request_uri/$request содержат токены скачивания; намеренно используем $uri.
log_format mdiag_safe '$remote_addr [$time_local] "$request_method $uri" $status $body_bytes_sent';
